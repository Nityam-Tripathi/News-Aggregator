version: "3.9"

services:
  db:
    image: ankane/pgvector
    container_name: news_db_v2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: newsdb_v2
    ports:
    - "5433:5432"